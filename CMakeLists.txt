cmake_minimum_required(VERSION 3.12)
set(CMAKE_CXX_STANDARD 17)
project(wlua)
add_library(wlua INTERFACE)
target_link_libraries(wlua INTERFACE lua5.3)
target_include_directories(wlua INTERFACE "public")
if(${BUILD_WLUA_EXAMPLES})
  add_subdirectory(examples)
endif()
